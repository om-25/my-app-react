pipeline {
    agent any

    stages{
       stage("scm checkout"){
          steps{
              checkout scm
          }
       }
      stage("build"){
          steps{
              sh "npm run build"
          }
       }
   }
}

