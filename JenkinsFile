pipeline {
    agent any

    stages{
       stage("scm checkout"){
          steps{
              checkout scm
          }
       }
      stage("test"){
          steps{
              sh "apt install npm"
              sh "npm test"
              
          }
       }
      stage("build"){
          steps{
              sh "npm run build"
          }
       }
    }
}
